#pragma once

int 取游戏状态();

bool 交易上架();

bool 是否在BOSS房();

bool 是否开门();

bool BOSS死亡();

bool 是否有物品();

bool 文件是否存在(const wchar_t* 文件名);

int 取角色等级();

int 取剩余疲劳();

bool 判断未央柱子();

int 判断当前角色位置();

int 取角色数量();